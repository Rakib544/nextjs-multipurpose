import Pagination from "@/components/ui/pagination";

## Pagination

Pagination with page navigation, next and previous links.

### Examples

---

<Pagination totalItems={100} perPageItems={15} />

#### View example's code

```js
import Pagination from "@/components/ui/pagination";
<Pagination totalItems={100} perPageItems={15} />;
```
